import 'package:flutter/material.dart';

import 'app_aula7.dart';

main(){
  runApp(const AppAula7());
}


/*
  Sequência atividade:
  - definir main 
  - Criar o nosso widget (uma nova classe)

  - definir runApp 
  - passar o widget criado como parâmetro (sem const)
  - voltar no widget criado para definir const

  - definir const na criação do nosso widget
  - criar a nossa primeura tela


  O que o main faz?
  O que runApp faz?
  O que é, para que serve e qual tipo de objeto gera o ensureInitialized?
  Que tipo de método é o ensureInitialized?
  Para que serve, quando devemos utilizar e que cuidados devemos ter com static?
  Em dart, para que serve a sintaxe em que usa ".."?
  O que é e para que serve o scheduleAttachRootWidget?
  O que é e para que serve o scheduleWarmUpFrame?
  */